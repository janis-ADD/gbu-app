/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  poweredByHeader: false
  // typedRoutes wird in Schritt 2B.3 reaktiviert, sobald alle Public-Routes
  // angelegt sind. Im MVP ohne typedRoutes-Pflicht, um Stub-Routen zu vermeiden.
};

export default nextConfig;
